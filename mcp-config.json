{
  "mcpServers": {
    "hostinger": {
      "command": "node",
      "args": ["dist/index.js"],
      "env": {
        "HOSTINGER_API_TOKEN": "${HOSTINGER_API_TOKEN}"
      },
      "disabled": false
    }
  }
}
